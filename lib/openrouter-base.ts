/** The OpenRouter API root. Its own module so the small consumers — the
 *  embeddings client, for one — do not import the whole chat pipeline. */
export const OPENROUTER_BASE = "https://openrouter.ai/api/v1";
